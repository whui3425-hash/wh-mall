# wh-mall


安装 Maven 和 JDK：
```shell
# Ubuntu
sudo apt install -y maven openjdk-17-jdk

# CentOS
sudo yum install -y maven java-17-openjdk-devel
```

方式A - Git 克隆（推荐）：

```shell
# Ubuntu/Debian
sudo apt install -y git
# CentOS/RHEL
sudo yum install -y git

git clone <你的仓库地址> /opt/mall
cd /opt/mall
```
